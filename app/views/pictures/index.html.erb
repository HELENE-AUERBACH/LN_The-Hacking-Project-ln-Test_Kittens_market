i<style type="text/css">
  table {
    margin:auto;
  }
  table,
  td {
    border: 1px solid #333;
  }

  thead,
  tfoot {
    background-color: #333;
    color: #fff;
  }
</style>

<%- content_for(:jumbotron_small_hero_title, "Venez acheter les photos de chaton(s) trop mignon(s) qui vous tentent!") %>
<%= render 'shared/small_banner' %>
<div>
  <h1>Bienvenue sur l'application "Kittens Pictures Market" de <em>"Iziteam"</em></h1>
  <BR>
  <% if !notice.nil? %>
    <div class="alert alert-success" role="alert">
      <p id="notice"><%= notice %></p>
    </div>
  <% end %>
  <table>
    <caption>Liste de toutes les photos</caption>
    <thead>
      <tr>
        <th>Titre</th>
        <th>Page</th>
      </tr>
    </thead>
    <tbody>
      <% @pictures.each do |picture| %>
        <tr>
          <td><%= picture.title %></td>
          <td><%= link_to "Photo n°#{picture.id}", picture_path(picture.id) %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
